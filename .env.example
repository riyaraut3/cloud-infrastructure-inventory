# Local development ONLY. Change the key; never put production secrets in Git.
API_KEY=change-this-local-demo-key
DATABASE_URL=postgresql+psycopg2://inventory:inventory_local_only@db:5432/inventory
CORS_ORIGINS=http://localhost:5173
LOCAL_UPLOAD_DIR=/data/uploads
